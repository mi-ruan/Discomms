import React from 'react';

const App = () => {
  return (
    <div id="App">
      <h1> Welcome to Discomms </h1>
    </div>
  )
};

export default App;

// start with authroute loginformcontainer if not logged in
// use "/login"
// put signup route here too but after loginform
//else protected route is main container
